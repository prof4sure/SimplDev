name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
   
    - name: Build the Docker image
      run: |
        cd /home/runner/work/SimplDev 
        ls -a 
        docker build . 
        
